#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "anhaengenanarray.h"
#define TRENNLINIE 60
#include "typen.h"

void anhaengenanarray(STRINGKETTE*pstart, char Buchstabe)
{

/* Übersicht
struct zeichenkette
{
    char zeichen;
    struct zeichenkette*next;
};
typedef struct zeichenkette* STRINGKETTE;
*/
/*
	if (*pstart == NULL)
	{
		*pstart = (struct zeichenkette*) malloc (1*sizeof(struct zeichenkette));
	//Fehlerprüfung
	(*pstart)->zeichen = Buchstabe;
	(*pstart)->next = NULL;
	return;
	}
	anhaengen((*pstart)->next, zeichen)
*/
}
